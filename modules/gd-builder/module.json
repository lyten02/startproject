{
  "name": "gd-builder",
  "version": "0.1.0",
  "description": "Build orchestrator — owns build.py, hxml generator, lint/test commands. Bootstraps build/ from templates on enable; disable is a no-op.",
  "dependencies": [],
  "lifecycle": {
    "clones": [
      { "from": "templates/profiles/main.json", "to": "build/profiles/main.json", "ifMissing": true },
      { "from": "templates/test.hxml", "to": "build/test.hxml", "ifMissing": true }
    ]
  }
}
