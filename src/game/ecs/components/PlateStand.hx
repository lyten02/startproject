package game.ecs.components;

/**
 * Marker: this Surface is a "clean plates" stand. The sink auto-delivers
 * washed plates here when a slot is free.
 */
class PlateStand implements Component {
	public function new() {}
}
